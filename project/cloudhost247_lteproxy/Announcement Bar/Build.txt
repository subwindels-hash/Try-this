Create a reusable Announcement Bar that displays scrolling messages at the top of the website.

Requirements:

Create a file: /templates/hostx/includes/announcementbar.tpl
Use only Smarty + HTML + CSS (optional minimal JS if needed)
Integrate it into the main layout (header.tpl) so it appears ABOVE the navbar
Display multiple announcement messages from a Smarty variable (array of messages with text + optional link)
Messages must scroll horizontally in a smooth infinite loop using ONLY CSS @keyframes
DO NOT use <marquee> or any external slider libraries
Ensure seamless looping with no gaps or jumps
On hover, pause the scrolling animation (animation-play-state: paused)
Each message should be clickable if it has a URL
Fully responsive for mobile, tablet, and desktop
Prevent text overflow and layout breaking
Keep design clean, modern, and consistent with WHMCS Hostx theme

Output:

Full announcementbar.tpl
Required CSS
Header integration snippet
Example Smarty array for announcements